(function (angular) {
  'use strict';
  // Generic checkout layout module — PSP-agnostic.
  // Lives here for now; ready to extract to its own extension when a second
  // PSP needs it (see AGENTS.md "prêt-à-extraire" strategy).
  angular.module('afCheckoutLayout', []);
})(angular);
